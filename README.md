# ask-linux 🐧🤖

A simple Linux CLI tool that lets you talk to an AI directly from the terminal.

Type `ask` before any sentence, and it will respond using a local LLM via Ollama.

## Usage
```bash
ask "who are you?"
